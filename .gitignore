# Contact-form submission log - contains personal data, never commit it
submissions.txt

# OS / editor cruft
.DS_Store
*.log

# Local secrets
.env
.env.*

# Private runtime data written outside the web root
private/
